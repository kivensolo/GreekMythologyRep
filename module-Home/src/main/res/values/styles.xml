<resources>

    <!-- 首页底部栏 tab样式 -->
    <style name="bottomTab">
        <item name="android:textSize">@dimen/size_10sp</item>
        <item name="android:layout_width">@dimen/size_0dp</item>
        <item name="android:layout_height">@dimen/size_40dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/color_divider</item>
        <item name="android:drawablePadding">2dp</item>
        <item name="android:background">@null</item>
    </style>

    <style name="hub_round_btn_style">
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
        <item name="android:background">@drawable/selector_hub_btn</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:textColor">@color/black</item>
    </style>
</resources>
