<resources>
    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>
    <!-- Application theme. -->
    <!--<style name="AppTheme" parent="AppBaseTheme">-->
         <!--All customizations that are NOT specific to a particular API-level can go here. -->
    <!--</style>-->

    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:listDivider">@drawable/divider_bg</item>
    </style>

    <style name="customstyle">
4         <item name="android:background">@android:color/holo_red_light</item>
5         [... or other style code...]
            <!--两种方式引用这个customstyle
             1:直接在布局文件该view标签里使用:style="@style/customstyle"
             2:在AndroidManifest.xml的application标签里使用:android:theme="@style/customstyle"-->
6     </style>

    <!-- 无Title的Dialog -->
    <style name="dialogNoTitle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:background">#00000000</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

  <!--  <style name="dialogAnimation">
        <item name="android:windowEnterAnimation">@anim/dialog_anim_in</item>
    </style>-->

</resources>
